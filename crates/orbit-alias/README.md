# orbit

This crate reserves the `orbit` name on crates.io.

The actual binary is published as [`orbt`](https://crates.io/crates/orbt).

## Install

```sh
cargo install orbt
```

Or use one of the other install methods at https://github.com/linuszz/orbt.
